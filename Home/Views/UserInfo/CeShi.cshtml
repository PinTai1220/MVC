
@{
    Layout = null;
}

<!DOCTYPE html>
<html>
<head>
    <meta charset="UTF-8">
    <!-- import CSS -->
    <link rel="stylesheet" href="https://unpkg.com/element-ui/lib/theme-chalk/index.css">
    <script src="https://cdn.jsdelivr.net/npm/vue"></script>
    <!-- 引入组件库 -->
    <script src="https://unpkg.com/element-ui/lib/index.js"></script>
    <!--axios发送ajax请求的插件-->
    <script src="~/JS/axios.js"></script>
</head>
<body>
    <div id='app' style="text-align:center">

        <span style="font-size:40px;">用户登录</span>
        <el-form ref="form" :model="form" label-width="80px"style="margin-top:250px;">
            <el-form-item>
                <el-input v-model="form.name" placeholder="请输入用户名" clearable style="width:480px;height:100px;font-size:24px;" ></el-input>
            </el-form-item>
            <el-form-item>
                <el-input v-model="form.region" placeholder="请输入密码" clearable style="width:480px;height:100px;font-size:24px;"></el-input>
            </el-form-item>
            <el-form-item>
                <el-button type="primary" v-on:click="onSubmit" style="font-size:22px;">登录</el-button>
                <el-button style="font-size:22px;">注册</el-button>
            </el-form-item>
        </el-form>
    </div>
    <script type="module">
        window.onload = function () {
            var Ctor = new Vue({
                el: '#app',
                data: {
                    form: {
                        name: '',
                        region: ''
                    }
                },
                methods: {
                    onSubmit() {
                        console.log(JSON.stringify(this.form));
                        //axios.post('url',this.form)
                        //.then(fucntion(response){
                        //提交成功回调
                        //})
                        //.catch(function(error){
                        //提交失败回调
                        //})
                    }
                },
                created: function () {
                    //一般来说这里会发送ajax请求
                    this.form = { };

                    //axios发送ajax
                    //axios.get('url').then(function(response){
                    //成功时回调 response响应的数据 赋值给this.user
                    //})catch(function(error){
                    //失败时回调 error失败时的错误消息

                    //})
                }
            });
        }
    </script>
</body>
</html>
